# Used by app/api/quote/route.ts to send quote-request emails via Resend.
# Set the real value in Hostinger's Node.js Web App environment variables —
# never commit it here.
RESEND_API_KEY=
NEXT_PUBLIC_SITE_URL=
